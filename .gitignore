# Render outputs are throwaway.
outputs/

# Redundant — the openscad-wasm/ folder is what we use.
openscad-wasm.tgz

# OS / editor noise
Thumbs.db
.DS_Store
*.swp
*~
.vscode/
.idea/

# Test harness — installed by `npm install`, not committed.
node_modules/
package-lock.json
test-results/
playwright-report/
capture-run.log
test-timings.ndjson
